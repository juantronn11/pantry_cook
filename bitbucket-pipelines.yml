image: node:18

pipelines:
  branches:
    dev3:
      - step:
          name: Deploy to Railway
          script:
            - npm i -g @railway/cli
            - railway up --service=$RAILWAY_SERVICE
